/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "www.salesforce.com",
            "static.vecteezy.com",
            "images.unsplash.com"
        ]
    }
};

export default nextConfig;
